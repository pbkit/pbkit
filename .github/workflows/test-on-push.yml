name: Build & Test on Push
on: push

jobs:
  build-pollapo:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: cli/pollapo
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Setup Deno
        uses: denoland/setup-deno@v1
        with:
          deno-version: v1.x
      - name: Build
        run: deno compile -A --unstable entrypoint.ts

  build-pb:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: cli/pb
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Setup Deno
        uses: denoland/setup-deno@v1
        with:
          deno-version: v1.x
      - name: Build
        run: deno compile -A --unstable entrypoint.ts

  test:
    runs-on: ubuntu-latest
    needs: [build-pollapo, build-pb]
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Setup Deno
        uses: denoland/setup-deno@v1
        with:
          deno-version: v1.x
      - name: Build
        run: deno test -A --unstable
